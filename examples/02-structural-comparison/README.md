# Structural Comparison Example

This example group demonstrates pairwise comparison for two profiles with the same declared `comparison_scope_label`.

It is the next step after source ingestion: two assessment results are generated first, then compared with or without an analyst-authored alignment file.

Humans can follow the commands manually. AI agents can execute the same sequence by treating the profiles and alignment as declared inputs and the comparison result as the next pipeline artifact.
