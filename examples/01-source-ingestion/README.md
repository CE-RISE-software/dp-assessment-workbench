# Source Ingestion Example

This example group demonstrates assessment and coverage runs from composition profiles that load SHACL sources by URL.

The purpose is to exercise source loading, profile parsing, and coverage execution. The specific model ecosystem is only an input to the example.

Humans can use this folder as the first command tutorial. AI agents can use it as a deterministic recipe: select one profile file, run `assess`, and inspect the JSON result.
